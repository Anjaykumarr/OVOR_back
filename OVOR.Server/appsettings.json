{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",

    "ConnectionStrings": {
        "Database": "Server=$(DB_SERVER);Port=$(PORT);Database=$(NAME);Uid=$(UID);Pwd=$(PASS);SslMode=Required;"
    }
}
